on:
  workflow_dispatch:

jobs:
  display-variables:
    runs-on: ${{ vars.RUNNER }}
    steps:
    - name: Use variables
      run: |
        echo "repository variable : $REPOSITORY"
      env:
        REPOSITORY_VAR: ${{ vars.REPOSITORY }}

        
